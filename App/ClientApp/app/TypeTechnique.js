var TypeTechnique = /** @class */ (function () {
    function TypeTechnique(id, length, point, isSelected, color, layer, width) {
        this.id = id;
        this.length = length;
        this.point = point;
        this.isSelected = isSelected;
        this.color = color;
        this.layer = layer;
        this.width = width;
    }
    return TypeTechnique;
}());
export { TypeTechnique };
//# sourceMappingURL=TypeTechnique.js.map